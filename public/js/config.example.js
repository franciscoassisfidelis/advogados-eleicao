// Copie este arquivo para "config.js" (mesma pasta) e preencha com os
// dados do seu projeto Supabase (Project Settings > API).
//
// A "anon key" é uma chave PÚBLICA por design — pode ficar no código do
// site estático. A segurança dos dados é garantida pelas políticas de
// Row Level Security (RLS) definidas em supabase/002_rls_policies.sql,
// não pelo sigilo desta chave. Nunca coloque a "service_role key" aqui.
export const SUPABASE_URL = "https://SEU-PROJETO.supabase.co";
export const SUPABASE_ANON_KEY = "SUA_CHAVE_ANON_PUBLICA";
